__pycache__/
*.pyc
render/
*.qet~

# 点検メモの Excel は書くための面で、git に載せるのは docs/点検メモ.md のほう。
# バイナリなので差分が出ず、2台のPCで別々に書き込むと突き合わせができない。
# 相手のPCでは py -3 tools/checklist.py --xlsx を叩けば同じものができる。
docs/点検メモ.xlsx

# 規格票をリポジトリに入れない。ここは公開リポジトリで、
# 買った JIS の PDF やそれを描き出した画像を置くと著作物の再配布になる。
# 意図して入れたい PDF があるときだけ git add -f する。
*.pdf
規格/
参照/
