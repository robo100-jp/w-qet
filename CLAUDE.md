# w-qet

QElectroTech 用の、日本の制御盤向け図記号ライブラリと作図ツール。GitHub 公開を前提とする。

**個人のリポジトリ。** 作者の勤務先とは関係がなく、勤務先を示す情報を入れない。
作者は板金・機械加工の現場で制御盤を扱う技術者（制御盤は改造案件が中心）。
開発は2台のPCで行い、同期は **git**（OneDrive ではない）。

**QElectroTech 公式でも、規格団体とも無関係な非公式プロジェクト。**
文書で「公式」「決定版」「JIS準拠」と読める書き方をしないこと。

---

## セッションの初めに [引継ぎ.md](引継ぎ.md) を読む

次に何をすればよいかが書いてある。**まずこれを読んでから動く。**

**区切りごとに書き直す。** 節をひとつ仕上げたとき、方針を変えたとき、
つまずいて解決したとき、そして作業を終えるとき。書き直したらコミットする。

途中で電源が落ちても続きから再開できるようにするための一枚なので、
**あとで書こうと思わずその場で書く。**

- 採録数のような**数えれば分かる数字は書かない**（`py -3 tools/status.py` が出す）
- 長い設計の話は `docs/` に書く。引継ぎ.md には**次の一手だけ**

---

## 絶対に守ること

### 1. 上流コレクションから派生させない

QElectroTech 同梱のコレクションから記号をコピー・変換して使わない。
**一部のフォルダだけの話ではない。**0.100.0 の同梱は
`10_electric` `20_logic` `30_hydraulic` `50_pneumatic` `60_energy` の 8597個で、
`ELEMENTS.LICENSE` は**コレクション全体**を対象にしている。

**IEC 60617 の番号で並べた `91_en_60617`（912個）も、その中に入っている。**
`10_electric` の下にあるので上の階層だけ見ると見落とすが、**同じ扱い。**
番号が実在するかの当たりを付けるのに読むのはよいが、**図は写さない。**

理由は `C:\Program Files\QElectroTech\ELEMENTS.LICENSE`。

> 回路図の**中**で使う・改変する・組み込む → 無条件
> 回路図の**外**で全部または一部を再配布する（改変の有無を問わず） → **CC-BY に従う**

シンボル集としての公開は後者に当たり、`Author: The QElectroTech team` の帰属義務が発生してライセンスを自由に選べなくなる。

**このプロジェクトの記号は JIS C 0617（＝IEC 60617）を参考に、座標から自分で描き起こす。**
図記号番号は参照として `<informations>` に書いてよいが、規格書の図版を複製しない。

### 2. 「JIS準拠」と名乗らない

適合宣言と受け取られうる。**「JIS C 0617 を参考に作成」**とする。

### 3. 記号を作ったら必ず画像化して目視する

```bash
py -3 tools/render_elmt.py <部品名>
py -3 tools/check_elmt.py <部品名>     # 目視では出ない誤り（外形・端子・属性）
py -3 tools/sympage.py                # 記号を足したり直したら docs/sym/ も作り直す
```

**座標を読むだけでは形の誤りに気づけない。** 過去の実例：

- ブザーのリードを弧の側から出していた（正しくは平らな面＝弦から2本）
- 直流モーターの直流記号が実線1本だけだった（正しくは実線＋その下に破線）
- 横向きにしたとき**ラベル（機器記号）まで倒した**。ラベルは倒しても正立のままが正しい。
  `<dynamic_text>` に `keep_visual_rotation="true"`（実機で確認済み）

> **記号の中に描いた文字（`Θ` `V` `Fe` `I >` など）は別。** あれは `<text>`＝図形の一部なので、
> 記号を回せば一緒に倒れる。**それでよいと決めてある**（QET 同梱コレクションも94%が同じ）。
> [docs/寸法基準.md](docs/寸法基準.md) の「文字は倒れない」を見ること。

### 4. 顧客情報を持ち込まない

このリポジトリは公開する。取引先の回路・配線表・図面・部品表・価格を**絶対に入れない**。
**案件名・案件番号・図面のページ番号も入れない。** 社内の符丁や通称も、顧客の手がかりになる。
**勤務先の名前も入れない。**
別案件のフォルダからファイルをコピーするときは中身を確認すること。

### 5. 規格票を読んで分かったことは、必ずその場で文書に残す

**規格票は家PCにしかなく、セッションが終われば読んだ内容は消える。**
書き残さなければ、次のセッションは同じページをもう一度測り直すことになる。
実際、測り方の落とし穴を**同じ穴に2回**踏んだ。

**書く場所を決めてある。「あとで書く」ではなく、その記号を作った流れで書く。**

| 分かったこと | 書く場所 |
|---|---|
| その記号だけの事情（規格票の補足事項にある使い方の制約、端子を足したこと、値が例示であること） | その `.elmt` の `<informations>` |
| 寸法・文字の大きさ・線・書体の決めごと | [docs/寸法基準.md](docs/寸法基準.md) |
| 規格票の測り方、道具の使い方 | [docs/ツール.md](docs/ツール.md) と道具の中のコメント |
| 踏んだ落とし穴、二度と踏まないための注意 | `.claude/skills/jis-symbol/SKILL.md` |
| 次に何をするか | [引継ぎ.md](引継ぎ.md) |

**とくに規格票の説明欄は、図だけ見ていると落とす。**
補足事項（図に描かれている部品の一覧）・形状分類（図を構成する形）・
注釈（他の記号と共通の制約）を読み、そこにある制約を `<informations>` に書く。
過去に、補足事項にある部品を丸ごと描いていなかったこと（07-13-12）、
形状分類の「点」＝黒丸を落としていたこと（07-15-16）がある。

> **規格票の文をそのまま写さない。要点を自分の言葉で書く。**
> 図版と同じく本文も著作物で、写せば複製になる（上の 1・2）。

### 6. 人が書いたものを機械で消さない

`docs/sym/*.html` は `sympage.py` が作り直す生成物だが、
**その中の一部は依頼者が直接書き込む。**

```html
<!--hand:memo-->  ここは人が書く  <!--/hand:memo-->
```

`memo`（覚え書き）・`cad`（CAD用シンボルにするか）・`combine`（組合せ）の3つ。
`sympage.py` は書き出す前に既存の頁から中身を読み戻すので、作り直しでは消えない。

- **印（`<!--hand:…-->`）を消さない。** 消えると読み戻せない
- **`docs/sym/` を消してから作り直さない。** 人の書き込みごと消える
- 生成の仕組みを変えるときは、**読み戻しが効いているか確かめてから**流す
  （`py -3 tools/sympage.py` を2回叩いて `手書きを引き継いだ頁` の数を見る）

---

## 構成

```
引継ぎ.md        次に何をするか（セッションの初めに読む・区切りごとに書き直す）
elements/        .elmt 図記号定義
tools/
  paths.py         図記号と規格票 PDF の在りかを解決する（他が使う）
  elmtgen.py       .elmt を座標から組み立てるライブラリ
  qetgen.py        .qet をコードから生成するライブラリ
  render_elmt.py   .elmt を PNG に描き出す（目視確認用）
  check_elmt.py    .elmt を検証する（外形・端子・属性。目視では出ない誤り）
  check_qet.py     .qet を検証する
  stdpage.py       規格票のページから図の帯と作図モジュールの格子を取り出す
  compare_page.py  描いた記号を規格票と突き合わせる
  pdf_page.py      PDF のページを画像に描き出す
  mkindex.py       図記号番号→ページの索引を規格票から作り直す（手で作らない）
  status.py        採録状況を elements/ から数え直す
  svg_elmt.py      .elmt を SVG に写す（人に見せる姿）
  sympage.py       図記号1個1枚の HTML と目次・注釈のページを作る（docs/sym/）
  cheatsheet.py    印刷用の早見表を作る（docs/チートシート.svg）
  install.py       QET のユーザーコレクションに登録する
docs/            設計方針・採録状況・寸法基準・測定メモ・QETのXML
  sym/             図記号1個1枚の HTML＋目次＋注釈（公開用。sympage.py が作る）
  規格データ/       規格票から採った作業用のデータ（文書ではない）
    第<N>部索引.tsv    図記号番号→規格票のページ（mkindex.py が作る）
    第<N>部注釈.tsv    規格の注釈の**要点を自分の言葉で**書いたもの
render/          描き出した PNG・SVG（git 管理外）
.claude/skills/  Claude Code スキル
```

## 前提環境

- Windows / QElectroTech 0.100.0
- Python 3.9（`py -3`）／`pillow` が必要（`py -3 -m pip install pillow`）
- ユーザーコレクション: `%APPDATA%\qelectrotech\QElectroTech\elements\`
- **QET はスタートメニューから起動する。** exe 直叩きは部品0個になる

## 作図の知見

スキルに詳しくまとめてある。**記号を作るときは `jis-symbol` を読む。**

| スキル | いつ使う |
|---|---|
| `jis-symbol` | **図記号を `.elmt` に描き起こす**とき。規格票の測り方、QET で表示が壊れる条件 |
| `qelectrotech` | **回路図（`.qet`）を描く・生成する**とき。ラベルと相互参照、図面XML |

以下は要点だけ。

- 導体は**端子と端子の間にしか引けない**。ジャンクション部品は存在しない
- 部品定義の `link_type` は `master`（コイル）/`slave`（接点）
- **スレーブのラベルはマスタから同期される。** ただし記号に文字の欄（`ElementInfo`／`label`）が要る
- 接点に独自表示をしたいなら `text_from="UserText"`（日本の慣習の小文字はこちら）
- 図面XMLの `text` 属性は Qt リッチテキスト。**生の `"` を入れるとXMLが壊れる**
- 日本の慣習：コイルは大文字（T1）、その接点は小文字（t1）

## 日本語について

やりとりは日本語。コメント・ドキュメント・記号名も日本語で書く。
`.elmt` の名称は `ja` を主とし、`en` も併記する（海外の利用者向け）。
