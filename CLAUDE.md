# w-qet

QElectroTech 用の、日本の制御盤向け図記号ライブラリと作図ツール。GitHub 公開を前提とする。

**個人のリポジトリ。** 作者の勤務先とは関係がなく、勤務先を示す情報を入れない。
作者は板金・機械加工の現場で制御盤を扱う技術者（制御盤は改造案件が中心）。
開発は2台のPCで行い、同期は **git**（OneDrive ではない）。

**QElectroTech 公式でも、規格団体とも無関係な非公式プロジェクト。**
文書で「公式」「決定版」「JIS準拠」と読める書き方をしないこと。

---

## セッションの初めに [引継ぎ.md](引継ぎ.md) を読む

次に何をすればよいかが書いてある。**まずこれを読んでから動く。**

**区切りごとに書き直す。** 節をひとつ仕上げたとき、方針を変えたとき、
つまずいて解決したとき、そして作業を終えるとき。書き直したらコミットする。

途中で電源が落ちても続きから再開できるようにするための一枚なので、
**あとで書こうと思わずその場で書く。**

- 採録数のような**数えれば分かる数字は書かない**（`py -3 tools/status.py` が出す）
- 長い設計の話は `docs/` に書く。引継ぎ.md には**次の一手だけ**

---

## 絶対に守ること

### 1. 上流コレクションから派生させない

QElectroTech 同梱の `91_en_60617`（IEC 60617 規格記号 約900種）から記号をコピー・変換して使わない。

理由は `C:\Program Files\QElectroTech\ELEMENTS.LICENSE`。

> 回路図の**中**で使う・改変する・組み込む → 無条件
> 回路図の**外**で全部または一部を再配布する（改変の有無を問わず） → **CC-BY に従う**

シンボル集としての公開は後者に当たり、`Author: The QElectroTech team` の帰属義務が発生してライセンスを自由に選べなくなる。

**このプロジェクトの記号は JIS C 0617（＝IEC 60617）を参考に、座標から自分で描き起こす。**
図記号番号は参照として `<informations>` に書いてよいが、規格書の図版を複製しない。

### 2. 「JIS準拠」と名乗らない

適合宣言と受け取られうる。**「JIS C 0617 を参考に作成」**とする。

### 3. 記号を作ったら必ず画像化して目視する

```bash
py -3 tools/render_elmt.py <部品名>
```

**座標を読むだけでは形の誤りに気づけない。** 過去の実例：

- ブザーのリードを弧の側から出していた（正しくは平らな面＝弦から2本）
- 直流モーターの直流記号が実線1本だけだった（正しくは実線＋その下に破線）
- 横向きにしたとき文字まで倒した（**文字・識別記号は倒しても正立のまま**が正しい）

### 4. 顧客情報を持ち込まない

このリポジトリは公開する。取引先の回路・配線表・図面・部品表・価格を**絶対に入れない**。
**案件名・案件番号・図面のページ番号も入れない。** 社内の符丁や通称も、顧客の手がかりになる。
**勤務先の名前も入れない。**
別案件のフォルダからファイルをコピーするときは中身を確認すること。

---

## 構成

```
引継ぎ.md        次に何をするか（セッションの初めに読む・区切りごとに書き直す）
elements/        .elmt 図記号定義
tools/
  paths.py         図記号の在りかを解決する（他が使う）
  elmtgen.py       .elmt を座標から組み立てるライブラリ
  qetgen.py        .qet をコードから生成するライブラリ
  render_elmt.py   .elmt を PNG に描き出す（目視確認用）
  check_qet.py     .qet を検証する
  measure_page.py  規格票のドット格子を測る
  pdf_page.py      PDF のページを画像に描き出す
  status.py        採録状況を elements/ から数え直す
  install.py       QET のユーザーコレクションに登録する
docs/            設計方針・採録状況・寸法基準・測定メモ
render/          描き出した PNG（git 管理外）
.claude/skills/  Claude Code スキル
```

## 前提環境

- Windows / QElectroTech 0.100.0
- Python 3.9（`py -3`）／`pillow` が必要（`py -3 -m pip install pillow`）
- ユーザーコレクション: `%APPDATA%\qelectrotech\QElectroTech\elements\`
- **QET はスタートメニューから起動する。** exe 直叩きは部品0個になる

## 作図の知見

`.claude/skills/qelectrotech/SKILL.md` に詳しくまとめてある。要点だけ。

- 導体は**端子と端子の間にしか引けない**。ジャンクション部品は存在しない
- 部品定義の `link_type` は `master`（コイル）/`slave`（接点）
- **スレーブのラベルはマスタから自動同期される。** 接点に独自表示をしたいなら `text_from="UserText"`
- 図面XMLの `text` 属性は Qt リッチテキスト。**生の `"` を入れるとXMLが壊れる**
- 日本の慣習：コイルは大文字（T1）、その接点は小文字（t1）

## 日本語について

やりとりは日本語。コメント・ドキュメント・記号名も日本語で書く。
`.elmt` の名称は `ja` を主とし、`en` も併記する（海外の利用者向け）。
