# 引継ぎ

**セッションの最初にこれを読む。終わりと区切りごとに書き直す。**

作業が途中で切れても（PC の電源が落ちる・スリープ・通信が切れる）
ここを読めば続きから再開できるようにしておくための一枚。

- **状態の数字は書かない。**採録数は `py -3 tools/status.py` が数え直す
- 長い設計の話は `docs/` に書く。ここは**次の一手だけ**
- 書き直したらコミットする

| 見るもの | どこ |
|---|---|
| 全体の方針・買う順番 | [docs/作業計画.md](docs/作業計画.md) |
| **どんな記号があるか（姿と諸元）** | [docs/sym/index.html](docs/sym/index.html)・[docs/チートシート.svg](docs/チートシート.svg) |
| **規格の注釈（使い方の決めごと）** | [docs/sym/notes.html](docs/sym/notes.html) |
| どこまで採録したか | `py -3 tools/status.py`（[docs/採録状況.md](docs/採録状況.md) を作り直す） |
| 寸法・端子・ラベルの約束 | [docs/寸法基準.md](docs/寸法基準.md) |
| **XML の構造（`.elmt`・`.qet`）** | [docs/QETのXML.md](docs/QETのXML.md) |
| 測ったがまだ描いていないもの | [docs/測定メモ.md](docs/測定メモ.md) |
| 規格票の扱いと置き場所 | [docs/規格の参照.md](docs/規格の参照.md) |
| 道具の使い方 | [docs/ツール.md](docs/ツール.md) |
| 描いたあとの点検 | `py -3 tools/render_elmt.py <名前>`（目視）＋ `py -3 tools/check_elmt.py`（機械） |

---

## いま何をしているか

**この回（2026-08-16・家PC）は会社の作業を取り込んだだけで、記号は触っていない。**
残っている動きは**入れ直しひとつ**（下の「入れ直しかた」）。そのあとは第3部を買う。

> **取り込んだのは「名前を現場の言い方にした」11コミット。**
> 頁に `desc`（部品の説明）欄が増え、`sympage.py` を叩き直しても
> 手書き252枚が全部残ることを確かめた（CLAUDE.md の6）。
> 追いついていなかった `docs/採録状況_第8部.md` だけ作り直した。

**第8部（67個）は描き終えた。第7部は 161/180 で、19個が残っている。**

> **索引が19個ぶん足りていなかった。** 手で作った `docs/規格データ/第7部索引.tsv` が
> 附属書A の途中で切れていて、`status.py` が **161/161 と嘘の採録率**を出していた。
> 規格票から作り直して 180個と分かった（`py -3 tools/mkindex.py --part 7`）。
> **残り19個は全部 旧図記号**（07-A11 多段スイッチの例10・07-A12 複合スイッチ3・
> 07-A13 1・07-A15 作動装置5）で、新しい図面には使わないもの。
> **分母を手で作らない。**`mkindex.py` を使う。

規格票との突き合わせは、描いてある 161＋67 について全件終わっている。
附属書A の旧図記号も入れてある。**旧図記号は新しい図面に使わないもの**なので
`07-A` / `08-A`（表示名「旧図記号（参考・使わない）」）に分けて置き、
`<informations>` にもそう書いてある。

**第8部まで含めて実機で確認済み**（2026-08-15・会社PC。下の「実機で確かめ終わったこと」）。
**次に買うのは第3部**（→ 第2部 → 第6部。理由は下の「次の一手（家）」）。

節をひとつ仕上げるたびに `status.py` → コミット → プッシュ。
**次に行ってよいか聞かない。**依頼者が不在のまま進めることを前提にしている。

`elements/drawing_aids/` に**作図用部品を8つ**置いた——
端子5つ（通過・T字分岐・十字・終端・L字）と、
**フォリオ参照 入／出・汎用ブロック**（実際の図面で足りなかったもの）。

> **「規格に基づく図記号ではない」と決めつけていたが、端子類は第3部にありそう。**
> 下の「次の一手（家）」を見ること。**第3部を買って確かめる。**

機械でできる点検は `check_elmt.py` を作って通してある。

**`install.py` は依頼者自身のターミナルで実行してもらうこと。**
Claude Code の環境から叩くと、リポジトリの外（`%APPDATA%`）への書き込みが
サンドボックスに閉じてしまい、**実ディスクには1つも届かない。**
たちの悪いことに書いた直後に読み返すと自分が書いたものが見えるので、
**成功したように見える。** 読み取りは素通しなので QET のログは信用してよい。

**QET の実機で一通り確認し終えた。**

| 見たこと | 結果 |
|---|---|
| 部品パネルに日本語で並ぶ | ✔ プレビュー失敗0 |
| 端子に導体がつながる | ✔ 端子を足したもの（07-17・07-27）も分岐も通った |
| 回してもラベルが正立 | ✔ `keep_visual_rotation="true"` が効いている |
| 記号の中の文字 | 一緒に倒れる。**それでよいと決めた**（[docs/寸法基準.md](docs/寸法基準.md)） |
| **9pt に上げた文字のはみ出し** | ✔ 縁までの余裕が最小の 07-09-01（1.28）で軌跡が残らず。**決着** |

**つまり第7部の本体（附属書Aを除く）は、実際に図面が描ける状態になっている。**

入れ直しと取り除きは下の「次の一手」に2つ並べてある（**依頼者に叩いてもらう**）。
**フォルダ構成を変えたときは先に `--uninstall`**、でないと古い名前のフォルダが残って二重に並ぶ。

QET のログ（`%APPDATA%\qelectrotech\QElectroTech\<日付>.log`）は読める。
起動ごとに `Custom Elements count` と警告が出るので、**入れ直しの結果はここで確かめられる。**

## どちらの PC にいるか、最初に確かめる

作業は2台の PC で進み、セッション名がそれを表す——**「QET 家」と「QET 会社」。**
git で同期しているのでリポジトリの中身は同じだが、**規格票の PDF は家PCにしかない**
（購入者名入りの透かし付きで、他の PC へのコピーが認められていない）。

```bash
py -3 -c "import sys;sys.path.insert(0,'tools');import paths;print(paths.standard())"
```

これが `FileNotFoundError` になったら**会社PC**。

| | 家 | 会社 |
|---|---|---|
| 規格票を測る（`stdpage.py`・`compare_page.py`・補足事項を読む） | ✔ | **できない** |
| 新しい記号を描き起こす | ✔ | **できない** |
| QET の実機で見る・`check_elmt.py`・ツールと docs の整備 | ✔ | ✔ |

**会社PC で規格票が要る作業を始めない。** 測れないまま座標を書くことになる。

## 入れ直しかた（**依頼者に叩いてもらう**）

```bash
py -3 tools/install.py
```

```bash
py -3 tools/install.py --uninstall
```

**この2つはいつも並べて出す。** フォルダ構成を変えたときは先に `--uninstall`。
コレクションの表示名は「電気用図記号（日本）」。

> **会社PC は最新の状態で入っている**（2026-08-15 夜。名前を替えたあとに
> 入れ直して再起動、267個・プレビュー失敗0 を確認済み）。
> **家PC は 2026-08-16 に pull しただけで、まだ入れ直していない。**
> 名前を替える前の古い和名が並んでいるはずなので、
> **`--uninstall` してから `install.py`**（節の表示名も替わっている）。

**登録される数は `elements/` の数と違う。**「CADにしない」と決めたものは入らない。
252個のうち**234個**が入る（旧図記号と使用例の18個を除く）。
`07-A` / `08-A` の節はユーザーコレクションに作られない。

## 実際の図面で置き換えを試した —— 足りないものが分かった

既存の制御盤の図面（3枚・部品113個）を、このライブラリの記号に
置き換えられるかで調べた。**結果は下の表。**

> **調べた図面そのものはリポジトリに入れない。** 依頼元の回路なので、
> 持ち込んでよいのは「どの図記号が足りないか」だけ（CLAUDE.md の4）。

| 足りなかったもの | その図面での数 | どこの記号か | 状態 |
|---|---|---|---|
| フォリオ参照 入／出 | 6 | **規格外**（IEC 61082 の領域） | ✔ 作った |
| 汎用ブロック | 3 | **第2部 02-01-06 かも**（未確認） | ✔ 作った |
| **表示灯（3色）** | 3 | **第8部** | ✔ 作った（08-10-01__green ほか） |
| **ブザー** | 1 | **第8部** | ✔ 作った（08-10-10。08-10-06 は音響信号装置の一般図記号） |
| **直流モーター** | 1 | **第6部** ¥15,180 | **未購入** |
| **整流器（AC-DC）** | 1 | **第6部**（同上） | **未購入** |
| ソレノイド | 1 | 07-15-01（作動装置）で代用できる | — |

**いま 111/113＝98% が置き換わる。**第6部を足して 100%。

### 分かったこと

- **押しボタン・スナップSW を `simple` にした判断は正しかった。**
  その図面の相互参照は**コイル → 接点の14本だけ**で、押しボタンもスナップSWも
  `master` と宣言されているだけでスレーブを従えていなかった。置き換えても参照は切れない
- ラベルは**コイルが大文字・接点が小文字**で、[docs/寸法基準.md](docs/寸法基準.md)
  に書いた慣習どおりだった
- **横向きの記号は別ファイルにしなくてよい。** 既存図面は「a接点_横」のように
  別部品を持っていたが、こちらは QET で回せば済む

### 押しボタンのブレーク接点 —— **決着（規格票で確認済み）**

**第7部にこの組合せの図記号番号は無い。**番号が振られているのは4つだけ。

| 記号 | 操作子 | 接点 |
|---|---|---|
| 07-07-01 | 手動操作（一般）＝縦棒 | メーク |
| 07-07-02 | **押しボタン**＝縦棒＋上下の横棒 | メーク |
| 07-07-03 | 引きボタン | メーク |
| 07-07-07 | 手動操作（**一般**） | **ブレーク** |

規格は**操作子の限定図記号と接点を組み合わせる**建て付けなので、
07-07-02 の操作子を 07-07-07 の接点に付ければ規格の流儀どおりになる。
そう組んだものを **`elements/JIS_C_0617/07-07/07-07-02_07-07-07.elmt`** に置いた。

> **置き場所は組合せ元と同じ節。**探すときは節のフォルダを開くから、
> 別のところに隔離すると見つからない。**ただし番号は騙らない**——
> ファイル名を `_` でつなぎ、`<informations>` は番号の行ではなく
> 「`JIS C 0617 の組合せ 07-07-02 ＋ 07-07-07`」。
> `elmtgen.write(..., num=None, combo=(...))` が書き、`check_elmt.py` が照合する。
> 決めは [docs/寸法基準.md](docs/寸法基準.md)「規格に番号の無い組合せ」。

---

## 次の一手（家）—— 第3部を買う

**第8部は描き終え、実機の確認も済んだ。**次にやることは3つ。
**第3部・第2部が先で、第6部はそのあと**（理由は下）。

### 1. 第3部を買う（¥11,660）—— **第6部より先**

**`elements/drawing_aids/` の端子類は、第3部にある図記号かもしれない。**
該当しそうな番号に当たりが付いている。

> **出どころは古い調べで、いまは確かめられない。** かつての QET 同梱には
> `91_en_60617`（IEC 60617 の番号でフォルダ分け）があったが、
> **0.100.0 には無い**（`10_electric` ほか5つに変わっている）。
> 下の表は**当たりであって確認済みではない。**規格票で確かめる。

| 番号 | 名称 | うちの部品 |
|---|---|---|
| 03-02-01 | Junction; Connection point | `junction_pass` `junction_corner` |
| 03-02-04・-05 | T-connection | `junction_tee` |
| 03-02-06・-07 | Double junction of conductors | `junction_cross` |
| 03-01-14 | End of a conductor, not connected | `wire_end` |
| 02-01-06 | Boundary（第2部） | `block_generic` |

**順番を第6部より先にする理由。**実際の図面で置いた113個のうち
**61%（69個）が端子・分岐**だった。第6部で埋まるのは直流モーターと整流器の**2個**。
効きが2桁ちがう。

**買ったらやること。**規格票で図を確かめ、規格に沿うなら
`JIS_C_0617/03-02` へ移す（**`drawing_aids/` から出す**）。
`<informations>` は「規格の番号はまだ確かめていない」と書き替えてあるので、
確かめた時点で番号の行にする。

> **フォリオ参照だけは 60617 に無い。**あれは図面作成の規約で、
> IEC 61082（図面の作成）の領域。`drawing_aids/` に残る。

### 2. 第2部を買う（¥16,500）—— **要る理由が2つ出た**

| 何が作れるようになるか | いま何が困っているか |
|---|---|
| **オルタネイト（非自動復帰）の押しボタン** | 規格に番号が無く、07-07-02 の操作子に **02-12-08**（非自動復帰）を重ねる形になる。**制御盤では出るのに作れない** |
| **汎用ブロック** | `block_generic` は **02-01-06 Boundary** かもしれない（未確認） |
| 自動復帰／非自動復帰の限定図記号 | 02-12-07・02-12-08。旧図記号 07-A1-04・07-A1-05 の置き換え先 |

### 3. 第6部を買う（¥15,180）

直流モーターと整流器。これで実際の図面が 100% 置き換わる。**第3部・第2部のあと。**

### 4. 第7部の残り19個

旧図記号（`py -3 tools/status.py --next 20`）。
**CADにしないと決めてある**が、**「しない」は「描かない」ではない。**
`.elmt` は作ってよい（そこから SVG と頁が出る）。`install.py` が QET に
入れないだけ。規格票から起こせるなら起こす。

### 公開用の頁（docs/sym/）

**カタログは消して `docs/sym/` に移した。**図記号1個につき1枚の HTML ＋
目次（`index.html`）＋ 規格の注釈（`notes.html`）＋ 姿だけの SVG。
いずれ robo100.jp で公開する。

```bash
py -3 tools/sympage.py
```

- 座標は**機械可読 JSON にだけ**入れてある（表は出さない）。
  円は中心と直径、弧は中心と角度、と**形式に依らない形**に直してある
- **`<!--hand:memo-->` `<!--hand:cad-->` `<!--hand:combine-->` で挟んだ欄は
  人が手で書く。**作り直しても消えない（`sympage.py` が読み戻す）。**印を消さないこと**。
  `cad` と `combine` は「この図記号から何を作るか」で1つの節にまとめてある
- **`names` は「一般的な名称」。**規格の名称とは別に、現場・部品表・カタログでの言い方を書く（規格「安全開離機能接点」＝通じるのは「ミラー接点」）。**目次の絞り込みはここも見る**ので、「サーマル」「MCCB」で引ける
- **`cad` は「CAD用シンボルにするか」。**「しない」と書いてあれば作らなくてよい、
  という合図。**Claude が規格票から判断して書くことも、人が書くこともある。
  置き場は頁だけ**（分けると二重管理になる）。「する。」「しない。」で書き始める。
  **図と頁は、しないものにも作る**
- **決めてあるのは2つ** —— **旧図記号（附属書A）** と **使用例**（装置ではなく
  使い方を示す図）は**シンボル化しない**。`sympage.py` が旧図記号に既定で
  「しない」を焼き、`install.py` が「しない」を QET に入れない
- `combine` は**組合せだけでなく変種も書く欄**（ランプの色ちがい・多段スイッチの
  段数ちがいなど）。作った先のファイル名を書いておくと頁からたどれる
- **まだ描いていない図記号にも頁が出る**（索引にあって `.elmt` が無いもの）。
  図の代わりに「まだ図がありません」と出て、手で書く欄は使える。
  **これが無いと、描く前に「CAD用シンボルにするか」を書く場所が無い**
- 規格の注釈は `docs/規格データ/第<N>部注釈.tsv`（**要点を自分の言葉で**）と
  `docs/規格データ/第<N>部注釈_適用.tsv`（適用先）から作る。**規格票 PDF は要らない**ので
  会社PCでも作り直せる

---

## 第8部の記録（**済んだこと**。次の一手ではない）

### JIS C 0617-8:2024 —— 描き終えた（67/67）

`%USERPROFILE%\規格\` に置いてある。

```bash
py -3 tools/status.py --part 8            # 採録状況を数え直す
py -3 tools/compare_page.py --num --all --part 8
```

**規格票との突き合わせも全件終わった。67件すべて 0.2M 以内で一致。**
`docs/規格データ/第8部索引.tsv`（67個）は巻末の「注釈」の節にも図記号番号が並ぶので、
`図記号番号` の見出しがあるページだけ拾ってある。

**表示灯の色ちがい4つ**（緑・赤・黄・白）も 08-10 に入れてある。

### 第8部で分かったこと

- 枠は3種類。**どれも引出し線が無い**ので外形の縁に端子を足す
- 中の文字は 9pt で第7部と同じ。`*` は 14pt、肩の数字（`10³`）は 7pt。
  **下付き 6pt と同じだと決め込まない**（0.60M と 0.64M で違う）
- **矢羽根はページごとに開いた V 字と塗り三角の両方がある。**
  塗り三角は `<polygon>`（QET の `end="triangle"` は長さ＝半幅で寸法が合わない）
- **走査は 0.2M 以下の刻みで。**0.5M では弦・底辺・白帯・破線を4回見落とした
- **倍尺のページがある**（下）

### 倍尺のページ —— **これが一番あぶない**

規格票は小さい図記号を**2倍に拡大して刷り、図記号欄に `200 %` と付ける**
（規格票の「概要」に書いてある）。**格子のドットは拡大されない**ので、
気づかずに測ると**倍の大きさの記号を作る。**

第8部では p.58（08-10-02）の1枚だけ。`stdpage.py` が倍率を読んで
`mx`/`my` に掛け、`200 %` の文字も墨から外すようにした（`p.scale`）。

> これに気づく前は「08-10-02 のランプは 08-10-01 の倍の大きさ」と
> 引継ぎにもスキルにも書いていた。**実寸に直すと同じ Ø2.0M。**
> 説明欄の「再設定比は 130 %」のような数字＋% を拾わないよう、
> **図の帯の中にあるものだけ**を倍率と見ている。

### 規格票の説明欄の通し読みも終わった（第8部）

**形状分類の全件照合**（円・線・長方形・矢印…を、こちらの `.elmt` にある図形の
種類と突き合わせる）をかけた。4件が引っかかったが、**すべて空振り**——
波形を `<polygon>` の折れ線で描いている（08-02-10・08-03-03）、
白帯を塗った多角形で描いている（08-10-03）など、同じ形を別のプリミティブで
描いているだけだった。第7部のときと同じ結果。

**注釈と補足事項からは 23件出た。**`<informations>` に足してある。

| 何 | どこに効くか |
|---|---|
| `*` の置き換え（A00144・A00145・A00369） | 08-01-01/02/03。単位・量の文字記号は IEC 60027 から採る |
| 量の文字記号を下に添える（A00146）・2種類なら輪郭を並べる（A00147） | 08-01-01/02/03・08-04-14 |
| 積算計は遠隔計器にも使える／記録計と組み合わせてよい（A00148） | 08-01-03・08-04-03/10/11/14/15 |
| **上に載る長方形の数＝積算値の種類の数**（A00148） | 08-04-08・08-04-09。**数を変えるなら別ファイル** |
| エネルギーの向きは第2部 02-05-01〜08 で示してよい | 08-04-04〜07（4つの違いも書き分けた） |
| 加熱線は 05-07-06 の代わりに 05-A7-02 でもよい | 08-06-03/05・08-A6-02/03 |
| `*` は同期装置の文字（C/T/R ＋ D/R/T/X/B）。内円が回転子（A00173） | 08-A9-01/02 |
| ランプの色と種類の符号（A00174） | 08-10-01/02（下） |

> **注釈は番号しか書いていない。**中身は巻末の注釈の節にある。
> 番号を引き直さないと、A00174 のように**符号を自分で決めてしまう。**

### 表示灯の色 —— **決めた。塗りと符号の両方**

**QET は図面に置いた部品の色を変えられない**（`<element>` に色の属性が無い）。
色ちがいは別ファイルにするしかない。**緑・赤・黄・白の4つを作った。**

```
08-10-01.elmt         ランプ（一般図記号）   ← 規格どおり・色なし
08-10-01__green.elmt  表示灯（緑）＋符号 GN  ← 塗りと引出し線は規格外
08-10-01__red.elmt    ／ __yellow ／ __white   符号 RD ／ YE ／ WH
```

**符号は規格の注釈 A00174 のもの**（RD＝赤・YE＝黄・GN＝緑・BU＝青・WH＝白）。
最初 G・R・Y・W の1文字で作ってしまい、注釈を読んで直した。
**白黒で刷ると塗りが飛ぶので符号を併記する。**決めは
[docs/寸法基準.md](docs/寸法基準.md)「表示灯の色」。

---

## 次の一手（会社）—— 実際の図面を置き換える

**調べは済んでいる。あとは置き換えるだけ。** 上の表のとおり、
いま **111/113＝98%** が置き換わる。残るのは直流モーターと整流器の2個で、
どちらも第6部（未購入）。**規格票を待たずに始められる。**

引くときは [docs/sym/index.html](docs/sym/index.html)（全部の姿と諸元）と
[docs/チートシート.svg](docs/チートシート.svg)（よく使うものの A4 1枚）。
**気づいたことは [docs/sym/](docs/sym/) のその記号の頁に書く**
（`<!--hand:memo-->` で挟んだ欄。作り直しても消えない）。

---

## 名前を現場の言い方にした（2026-08-15・会社PC）

**QET のパネルは `<name lang="ja">` しか出さない。**「遅緩動作形継電器コイル」では
タイマを探している人に引けなかったので、**約150個の和名を替えた。**

```
作動装置（一般図記号）      → リレーコイル
遅緩動作形継電器コイル      → オンディレータイマのコイル
メーク接点（限時閉路）      → タイマの a接点（オンディレー）
熱動継電器で構成される作動装置  → サーマルリレー
ランプ（一般図記号）       → 表示灯
```

**節の表示名も13節替えた**（作動装置→コイル、開閉装置→接触器・遮断器、
ランプ・音響信号→表示灯・ブザー ほか）。英語名は規格の節題のまま。

**規格の名称は失わない。**索引 tsv の5列目・`<name lang="en">`・
`<informations>` の「規格の名称: …」の3か所にある。

規則と落とし穴は `.claude/skills/jis-symbol/SKILL.md` の
「`<name lang="ja">` は現場の言い方にする」。要点だけ。

- **長さで切らない。**切れて困るのは「区別が末尾にあるとき」。
  **括弧の前（名前の本体）だけ**を見る
- **説明文を採らない。**だから頁に **`desc`（部品の説明）欄を新設**して
  名称と説明を分けた（271枚中68枚に混ざっていた）
- **空欄の例文（`<p class="ph">`）を名前にしない。**重複判定で止まったから
  助かったが、1個だけなら例文がそのまま部品名になっていた
- **同じ名前が2つできる組は両方とも規格名で残す。**片方だけ替えるより紛れない
  （断路器＝07-13-06/07-13-10・ラッチングリレー＝07-15-14/07-15-23・
  近接センサ＝07-19-01/07-19-02）

> **括弧が閉じていない欄が2つあると書いたが、こちらが壊していた。**
> 振り分けを括弧の中の「。」で切っていた（`junction_pass`・`08-05-05`）。
> 直して**括弧の合わない欄は0件**。端子5つの名前の系統も揃えた。

**分類そのものは JIS のまま**と決めた。機器別（リレー／タイマ…）は
**a接点がリレーにもタイマにも接触器にも属する**ので必ず破綻する。
番号・索引・採録・突き合わせが全部 JIS の構造に乗っているので崩さない。
**探しやすさは名前と `docs/sym/` の絞り込みで作る。**

## 実機で確かめ終わったこと（会社PC）

**第8部まで含めて全部通っている。** もう一度なぞらなくてよい。

| いつ | 見たこと | 結果 |
|---|---|---|
| 2026-08-15 夜 | **名前を替えたあとの入れ直し** | ✔ 234個・`Custom Elements count: 267`・プレビュー失敗0 |
| 2026-08-15 | 第8部67個を含む**234個の登録** | ✔ `Custom Elements count: 267`（旧33＋234） |
| 〃 | **プレビューの失敗** | ✔ `QPainter … engine == 0` **0件** |
| 〃 | 表示灯の色ちがい4つ・**倍尺から起こした 08-10-02** | ✔ 問題なし |
| 2026-08-14 | 作図用部品3つ・ラベル・相互参照 | ✔ 下記 |

> **ログに `UNIQUE constraint failed: element_info.element_uuid` が出るが、
> ファイル側に重複は無い**（生成した図面も実務の図面も、置いた部品の uuid は
> 全件ユニークだと確かめた）。QET 内部のデータベースの話で `Debug:` 級。
> **実害が見えるまで追わない。**

フォリオ参照は**動く。**ただし順番がある。

1. **先に** プロジェクトの属性 →「新しいフォリオ」→「フォリオ参照のラベル」を
   **`%id-%l%c`** にする（`%f` は空になることがある）
2. **そのあとで**フォリオを作る。**この設定は既存のフォリオには効かない**
3. 札を置いて相手とリンクする（右クリック →「リンクする要素」）

詳しくは `.claude/skills/qelectrotech/SKILL.md` の「フォリオ参照」。

**ラベルと相互参照。**コイルを `R1` にすれば、ラベルを書いていない
接点にも `R1` が出て、脇に相手の位置 `(1-B4)` が付く。記号側に文字の欄
（`ElementInfo`／`label`）があるからで、**端子を持つ記号にはだいたい入っている**。
入っていないのは端子を持たない限定図記号と作図用の端子で、機器記号を書かないもの。

> **`.qet` を生成するとき、空の `<dynamic_texts />` を書かない。**
> **定義側のラベル欄まで打ち消される。**これで何も出なくなり、
> 「スレーブのラベルは同期されない」と2度読み違えた。
> **手で描くと動くのに生成すると動かないときは、まず生成側を疑う。**

相互参照の位置も **`%f` ではなく `%id`**（`qetgen.py` の `<xref>` は修正済み）。

> **推測で2回外した。**「線番が一致すれば結ぶ」も「0.100 では動かない」も違った。
> 既存フォリオで出ないのを見て「動かない」と結論づけたのが誤り。
> **依頼者が新しいフォリオを作って出た。**
> 公式文書（qelectrotech-doc の element/type）に `%id-%l%c` と
> 「New folio section」と書いてある。**手元で再現しないときは、
> その設定がいつ効くのか（新規だけか既存もか）を疑う。**

## 規格票の説明欄の通し読みも**終わった**（第7部）

**形状分類の全件照合**（点・線・長方形・円・矢印・三角形…を、こちらの `.elmt` に
ある図形の種類と突き合わせる）をかけた。22件が引っかかったが、**すべて空振り**
——同じ形を別のプリミティブで描いているだけだった（回した正方形を `<polygon>` で
描く、矢羽根を線2本で描く、など）。**07-15-16 の黒丸のような落としは他に無い。**

**補足事項からは2件出た。**「〜しなければならない」「〜だけに使用する」と
書いてあるのに `<informations>` が空だったもの。

- **07-13-15** 使わない機能の図記号素子は消すこと
- **07-13-17** 遮断と断路が一体でひとつの操作の機器にだけ使う

> **図だけ見ていると落とす。**説明欄には、図に描かれている部品の一覧（補足事項）、
> 図を構成する形（形状分類）、他の記号と共通の制約（注釈）が書いてある。

## 規格票との寸法の突き合わせも**全件終わった**（第7部）

寸法の全件走査に加えて、格子の無いページも目視で見比べた。**形はすべて一致。**
全180ページを走査したところ、格子が無いのは **10枚**——
**07-16-02〜07-16-10 の9枚**（07-16-01 と 07-16-11 には格子がある）と、
**07-A11-01**（p.184。図がページを使い切る旧図記号。まだ描いていない）。
**前は「第16節8個」と書いていた。**第8部に格子の無いページは無い。
残る「ちがう」は5件で、すべて理由が付いている。

**走査し直しても同じ6件で、増えていない**（第8部は0件）。

| 残っているちがい | 件数 | 理由 |
|---|---|---|
| 引出し線の長さ | 3 | 端子間 60 の決めごと（07-13-14・07-13-15・07-13-16） |
| 可動接片の角度 | 1 | 決めごと（07-25-01） |
| 規格票に図が2つ | 1 | 07-25-03。**両方描いた**が、突き合わせはページに2図写るのでずれる |
| 07-01-10 | 1 | 下記の保留 |

> **変種（`__` の付くもの）は突き合わせに出ない。** `compare_page.py` は
> 図記号番号でページを引くので、1つの番号につき1個しか見ない。
> 色ちがい・段数ちがい・向きちがいは**目視で確かめる。**

**07-01-10（断路遮断機能）だけ保留。** 規格票の自シートでは 1.26×1.04M だが、
同じ星を組み込んだ 07-13-17 では 1.0M。**規格票のほうが揃っていない。**
こちらは 1.0M にしてある（組み込んだときに揃うほうを採った）。

**書体は合わせないと決めてある。** 規格票は変数をセリフ斜体で組む（`U` `I` `P`）。
こちらは Liberation Sans。形は合わせるが書体は合わせない。

**道具はリポジトリに入れた**（この回で入れた。もう作り直さなくてよい）。

```bash
py -3 tools/stdpage.py 78 --rows        # 格子・墨の成分・行走査
py -3 tools/compare_page.py 07-13-17    # 規格票と並べる
py -3 tools/compare_page.py --num --all # 全件の数値。数分かかる
```

### 直した記録（この突き合わせで出たもの）

- **測定そのものが壊れていた。** 格子の1目を「間隔の中央値」で決めていて、
  p.78 で **1M が半分**、p.81 で **7% 大きく**出ていた。周期合わせに変えたら
  「要修正」と見ていた 07-13-17・07-22-02 は**どちらも正しかった**
- **07-13-14** 破線が円の中から出て、4極目の先まで伸びていた
- **07-15-16 / -17 / -23** 規格票の**黒丸2つ**を落としていた（形状分類が「点」）
- **07-13-12** **戻り止め付き手動引外し装置**を落としていた（補足事項に書いてある）
- **記号の中の文字が全体に小さすぎた。** 規格票はどのページも字面 0.90〜0.93M。
  6〜8pt が混ざっていたのを **66か所 9pt に**そろえた
- **07-12-06** 文字を大きくしたら行が重なった。箱が 2M 低く、行間も 0.95M（規格票 1.27M）
- **07-15-09** 箱が左に 0.5M 短かった
- **07-13-15** 上端の縦棒が 0.2M（規格票 0.65M）
- **描き出しの破線が QET の4倍細かかった**（刻み 0.25M 対 0.6M）。
  QET は Qt のペンそのままで**刻みを指定できない**。規格票は 1.0M だが、
  同梱コレクションも 918個が同じ指定で済ませているので据え置き
- `check_elmt.py` の文字判定を、送り幅の表から**実フォントの実測**に変えた

### 見つかったら

**生成し直さず XML を書き換える。** uuid が変わると既存の `.qet` の参照が切れる。

## そのあとの候補

第8部・第6部は上の「次の一手（家）」に書いた。そのさらに先。

- **第2部・第3部の常用記号**（限定図記号・端子台・接地）。
  [docs/規格の参照.md](docs/規格の参照.md) の段階2にあたる

宿題として残っていること。

- **オルタネイト（非自動復帰）の押しボタンが無い。** 規格に番号が無く、
  **07-07-02 の操作子に「非自動復帰」の限定図記号 02-12-08 を重ねる**形になる。
  **第2部が未購入なので、いまは作れない。**制御盤では出るので、第2部を買うときに作る。
  （07-07-02 は自動復帰＝モメンタリ。07-07-04 のひねりスイッチが非自動復帰）

- **9位置以上の多段スイッチ。** 07-11-04 に 2〜8位置がある。
  **上限は無い**ので、要るときに同じ規則で足す（生成スクリプトは使い捨てなので書き直す）。
  **07-11-05 は名称が「最大 4 位置」なので使えない。**07-11-04 のほうから作る
- 作図用部品の `link_type` は **`simple`** にした。[docs/寸法基準.md](docs/寸法基準.md) は
  `terminal` と書いているが、それだと端子台の一覧に分岐点まで並ぶと踏んだため。
  **端子台は QET 本体ではなく「端子台作成プラグイン」だった**ので当面は問題なさそう。
  実際にそのプラグインを使うときに、分岐点が一覧に出ないかを見る

## 一つ描くときの手順

1. `py -3 tools/status.py --next 10` で番号とページを出す
2. `py -3 tools/stdpage.py <ページ>` で測る。**規格票の説明欄も読む**
3. `tools/elmtgen.py` を使う生成スクリプトを**スクラッチパッドに**書いて `.elmt` を吐く
   置き場所は `elements/JIS_C_0617/<節>/<番号>_<和名>.elmt`（**節のフォルダ名はASCII**）
   節のフォルダを新設したら `qet_directory` も置く（表示名はここが持つ）
4. `py -3 tools/render_elmt.py <番号>` で**必ず目視**
5. `py -3 tools/check_elmt.py` と `py -3 tools/compare_page.py <番号>`
6. `py -3 tools/status.py` → `git commit` → `git push`

## 測る道具

規格票は**リポジトリの外**（`%USERPROFILE%\規格\`）。`.gitignore` で二重に弾いている。
在りかは `paths.standard()` が解決するので、**パスをソースに書かない。**

| | |
|---|---|
| `tools/stdpage.py` | 図の帯と格子を取り出す。ライブラリとしても使う |
| `tools/compare_page.py` | 記号を規格票と並べて寸法を数値で出す |

## 見せる道具（規格票が要らない。会社PCでも使える）

```bash
py -3 tools/sympage.py       # docs/sym/（1個1枚の頁・目次・注釈・SVG）
py -3 tools/cheatsheet.py    # docs/チートシート.svg（A4 1枚・印刷用）
```

**記号を足したり直したら `sympage.py` を叩く。**`status.py` が採録の「数」を
数え直すのと同じで、**一覧を手で書かない。**ただし `docs/sym/*.html` の
`<!--hand:…-->` で挟んだ欄は人が書くところで、**作り直しても消えない。**
チートシートは載せるものを `cheatsheet.py` の `GROUPS` が持っていて、
**A4 に収まらなくなると終了コード1で知らせる**（足すなら何かを落とす）。
第8部を載せるとき**始動器（07-14）と入れ替えた**——実際の図面には
表示灯とブザーのほうが多く出るため。いま 296mm / 297mm で**もう空きが無い。**

**気づいたことは `docs/sym/` の頁に書く。** 記号1個につき1枚あって、
姿と諸元を見ながら `<!--hand:memo-->` の欄に書き込める。**作り直しても消えない。**

> **点検メモ（`docs/点検メモ.md`・`checklist.py`）は消した。**
> 251行あって書き込みは0行——一度も使わないまま、頁の覚え書き欄と
> 役目が重なった。**書く場所が2つあると、どちらが本当か分からなくなる。**
> 「どれを見たか」の一覧が欲しくなったら、
> `checklist.py` を git 履歴から戻して**頁の memo 欄を読むだけの一覧**に
> 作り替えるのが筋（書く場所は増やさない）。

**使い捨ての測定スクリプトを書く前に `stdpage.py` を import する。**
`Page(78)` が帯・格子・墨の成分を持っている。素朴に書き直すと
下の落とし穴を踏み直す（実際に2回落ちた）。詳しくは [docs/ツール.md](docs/ツール.md)。

## つまずいた実例（同じ穴に落ちないため）

- **規格票の図は縦横で縮尺が違う。** 表のセルに入っているせいで列と行の間隔が
  最大6%ずれる。**必ず別々に測る**
- **格子の1目を「間隔の中央値」で決めてはいけない。** ドットは図に隠れて飛ぶし、
  図の小さな部品や表の切れ端がドットに混じる。中央値も最頻値もそれに引きずられ、
  **あるページで 1M が半分、別のページで 7% 大きく**出た。
  **周期そのものを合わせる**（Σ exp(2πi x/p) が最大の p を探す。`stdpage.py`）
- **図の帯は横罫線と横罫線のあいだ。**最初の罫線で切ると見出し行で切れる。
  帯をドットの**数**だけで選ぶと、説明表の句読点のほうを図と取り違える
- **`text` の中身はエスケープする。** `I >` の `>` を素で書くとタグの終わりと
  見分けがつかず、読み手が途中で切って**文字が丸ごと消える**
- **弧の angle が負のとき向きが逆になる。** Pillow は start から時計回りに end まで
  描くので、Qt の負角をそのまま渡すと反転する（`render_elmt.py` で対処済み）
- **可動接片の角度は規格票がページごとに揺れている。** 揃えると決めてある。
  [docs/寸法基準.md](docs/寸法基準.md) の表に従う
- **規格票にリード線が無い図記号がある。** 外形の縁に端子を足し、
  足したことを `<informations>` に必ず書く
- **目視だけでは外形の不備が残る。** 絵に出ないので気づけない。
  `check_elmt.py` を通したら、描き終えたつもりの166個から17件出た。
  **描いたら目視と `check_elmt.py` の両方をかける**
- **弧を外接矩形で判定すると必ずはみ出して見える。** 楕円の通らない側まで数えるため
- **描き出し側と生成側が同じ数式で間違えると、目視でも検査でも通ってしまう。**
  文字の `1em` を `0.78 × pt` としていた（正しくは `pt × 96/72`）。
  プレビューでは正しく見えて、**QET では1.7倍**になり外形からはみ出していた。
  **迷ったら同梱コレクションの統計と突き合わせる**（うちだけ 10〜18、同梱は 2〜9 だった）
- **文字が外形からはみ出すと、動かしたとき画面にゴミが残る。**
  QET は外形の矩形しか描き直さないため。見た目だけの問題ではない
- **リポジトリの外に書いたものは実ディスクに届かない**（上記）。
  QET のユーザーコレクションを「入れておきました」と言わないこと。**依頼者に叩いてもらう**
- **`elements/` 以下に日本語のファイル名・フォルダ名を作らない。** 実機で確かめた。
  ファイル名に日本語 → **記号名がファイル名（拡張子つき）に化ける**。
  フォルダ名に日本語 → **`qet_directory` が読めず名前が空欄**。
  和名は `.elmt` の中の `<name lang="ja">` と `qet_directory` が持つので表示は日本語のまま。
  `check_elmt.py` が弾く
- **外形 10×10 は使わない**（20×20 以上）。同梱8597個でも4個しかない外れ値。
  ただし**今回の「名前が化ける」の原因はこれではなかった**——原因はファイル名の日本語
- **症状を1つの原因で説明しようとしない。** 「フォルダ名が空」と
  「記号名がファイル名になる」は別々の原因だった。片方を直しても片方は残る
- **仮説を「外れ」と早々に切らない。** 依頼者の「日本語フォルダ名では？」が正解だった。
  こちらは一度否定していた
- **限定図記号の中をリード線が通らないことがある。** 07-19 のひし形は
  上下の頂点で線が切れている。**行走査で中心の x に暗い画素が無いか**を見れば分かる
- **測定メモを鵜呑みにしない。** 07-18・07-19 では控えの3か所（枠の下端・
  縦棒の長さ・被検出体の形）が実測と違った。描く前に**そのページをもう一度走査する**
- **`git status` の「変更あり」を数えない。** `sympage.py` を叩くと
  `docs/sym/` を LF で書き直すので、CRLF で取り出してある git から見ると
  **400枚以上が「変更あり」に見える。中身は同じ。**
  `git diff --stat` で見れば実際に違うものだけが出る（このときは1件だった）。
  **数に驚いて作り直しを疑う前に、まず `git diff` を見る**

## 触ってはいけないもの

- **`backup-before-scrub` ブランチ。**スクラブ前の履歴。**絶対にプッシュしない。**
  `git push --all` と `--mirror` を使わない
- 買った規格票の PDF と、そこから描き出した画像。リポジトリに入れない
