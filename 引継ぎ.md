# 引継ぎ

**セッションの最初にこれを読む。終わりと区切りごとに書き直す。**

作業が途中で切れても（PC の電源が落ちる・スリープ・通信が切れる）
ここを読めば続きから再開できるようにしておくための一枚。

- **状態の数字は書かない。**採録数は `py -3 tools/status.py` が数え直す
- 長い設計の話は `docs/` に書く。ここは**次の一手だけ**
- 書き直したらコミットする

| 見るもの | どこ |
|---|---|
| 全体の方針・買う順番 | [docs/作業計画.md](docs/作業計画.md) |
| **どんな記号があるか（姿）** | [docs/カタログ.md](docs/カタログ.md)・[docs/チートシート.svg](docs/チートシート.svg) |
| どこまで採録したか | `py -3 tools/status.py`（[docs/採録状況.md](docs/採録状況.md) を作り直す） |
| 寸法・端子・ラベルの約束 | [docs/寸法基準.md](docs/寸法基準.md) |
| **XML の構造（`.elmt`・`.qet`）** | [docs/QETのXML.md](docs/QETのXML.md) |
| 測ったがまだ描いていないもの | [docs/測定メモ.md](docs/測定メモ.md) |
| 規格票の扱いと置き場所 | [docs/規格の参照.md](docs/規格の参照.md) |
| 道具の使い方 | [docs/ツール.md](docs/ツール.md) |
| 描いたあとの点検 | `py -3 tools/render_elmt.py <名前>`（目視）＋ `py -3 tools/check_elmt.py`（機械） |

---

## いま何をしているか

**JIS C 0617 第7部 161個は描き終えた**（`py -3 tools/status.py` で確認できる）。
附属書A の旧図記号7個も入れてある。**旧図記号は新しい図面に使わないもの**なので
`07-A`（表示名「07-A 旧図記号（参考・使わない）」）に分けて置き、
`<informations>` にもそう書いてある。

節をひとつ仕上げるたびに `status.py` → コミット → プッシュ。
**次に行ってよいか聞かない。**依頼者が不在のまま進めることを前提にしている。

`elements/drawing_aids/` の5つ（通過・T字分岐・十字・終端・L字）も置いた。

機械でできる点検は `check_elmt.py` を作って通してある。

**`install.py` は依頼者自身のターミナルで実行してもらうこと。**
Claude Code の環境から叩くと、リポジトリの外（`%APPDATA%`）への書き込みが
サンドボックスに閉じてしまい、**実ディスクには1つも届かない。**
たちの悪いことに書いた直後に読み返すと自分が書いたものが見えるので、
**成功したように見える。** 読み取りは素通しなので QET のログは信用してよい。

**QET の実機で一通り確認し終えた。**

| 見たこと | 結果 |
|---|---|
| 部品パネルに日本語で並ぶ | ✔ プレビュー失敗0 |
| 端子に導体がつながる | ✔ 端子を足したもの（07-17・07-27）も分岐も通った |
| 回してもラベルが正立 | ✔ `keep_visual_rotation="true"` が効いている |
| 記号の中の文字 | 一緒に倒れる。**それでよいと決めた**（[docs/寸法基準.md](docs/寸法基準.md)） |
| **9pt に上げた文字のはみ出し** | ✔ 縁までの余裕が最小の 07-09-01（1.28）で軌跡が残らず。**決着** |

**つまり第7部161個は、実際に図面が描ける状態になっている。**

入れ直しと取り除きは下の「次の一手」に2つ並べてある（**依頼者に叩いてもらう**）。
**フォルダ構成を変えたときは先に `--uninstall`**、でないと古い名前のフォルダが残って二重に並ぶ。

QET のログ（`%APPDATA%\qelectrotech\QElectroTech\<日付>.log`）は読める。
起動ごとに `Custom Elements count` と警告が出るので、**入れ直しの結果はここで確かめられる。**

## どちらの PC にいるか、最初に確かめる

作業は2台の PC で進み、セッション名がそれを表す——**「QET 家」と「QET 会社」。**
git で同期しているのでリポジトリの中身は同じだが、**規格票の PDF は家PCにしかない**
（購入者名入りの透かし付きで、他の PC へのコピーが認められていない）。

```bash
py -3 -c "import sys;sys.path.insert(0,'tools');import paths;print(paths.standard())"
```

これが `FileNotFoundError` になったら**会社PC**。

| | 家 | 会社 |
|---|---|---|
| 規格票を測る（`stdpage.py`・`compare_page.py`・補足事項を読む） | ✔ | **できない** |
| 新しい記号を描き起こす | ✔ | **できない** |
| QET の実機で見る・`check_elmt.py`・ツールと docs の整備 | ✔ | ✔ |

**会社PC で規格票が要る作業を始めない。** 測れないまま座標を書くことになる。

## 次の一手

**まず入れ直す。**コレクションの表示名を「電気用図記号（日本）」に改めた。
フォルダ名（`w-qet`）は変えていないので `--uninstall` は要らない。
入れ直すまで部品パネルには**古い名前が出たまま**になる。

```bash
py -3 tools/install.py
```

取り除くとき。**フォルダ構成を変えたときは先にこれを叩く**（でないと古い名前の
フォルダが残って二重に並ぶ）。

```bash
py -3 tools/install.py --uninstall
```


## 実際の図面で置き換えを試した —— 足りないものが分かった

既存の制御盤の図面（3枚・部品113個）を、このライブラリの記号に
置き換えられるかで調べた。**結果は下の表。**

> **調べた図面そのものはリポジトリに入れない。** 依頼元の回路なので、
> 持ち込んでよいのは「どの図記号が足りないか」だけ（CLAUDE.md の4）。

| 足りなかったもの | その図面での数 | どこの記号か | 状態 |
|---|---|---|---|
| フォリオ参照 入／出 | 6 | **規格外**（作図用部品） | ✔ 作った |
| 汎用ブロック | 3 | **規格外**（作図用部品） | ✔ 作った |
| **表示灯（3色）** | 3 | **第8部** ¥11,660 | **未購入** |
| **ブザー** | 1 | **第8部**（同上） | **未購入** |
| **直流モーター** | 1 | **第6部** ¥15,180 | **未購入** |
| **整流器（AC-DC）** | 1 | **第6部**（同上） | **未購入** |
| ソレノイド | 1 | 07-15-01（作動装置）で代用できる | — |

**作図用部品3つを作った時点で 106/113＝94% が置き換わる。**
第8部を足して 97%、第6部まで足して 100%。

### 分かったこと

- **押しボタン・スナップSW を `simple` にした判断は正しかった。**
  その図面の相互参照は**コイル → 接点の14本だけ**で、押しボタンもスナップSWも
  `master` と宣言されているだけでスレーブを従えていなかった。置き換えても参照は切れない
- ラベルは**コイルが大文字・接点が小文字**で、[docs/寸法基準.md](docs/寸法基準.md)
  に書いた慣習どおりだった
- **横向きの記号は別ファイルにしなくてよい。** 既存図面は「a接点_横」のように
  別部品を持っていたが、こちらは QET で回せば済む

### 要確認（規格票が要る・家PC）

**押しボタンのブレーク接点**に当たる図記号が第7部161個の中に見当たらない。
`07-07-07 手動操作スイッチ（ブレーク接点）` はあるが押しボタンではない。
**規格が押しボタン＋ブレーク接点をどう組ませているか**を第7部の規格票で確かめる
（限定図記号を重ねる形かもしれない）。実務では必ず出るので、無いままにできない。

---

## 次の一手（家）—— 規格票を買って足りない記号を作る

**買う順番は 第8部 → 第6部。** 上の表のとおり、第8部のほうが実務で先に要る。

### 1. 第8部 JIS C 0617-8:2024（¥11,660）

[JSA Webdesk](https://webdesk.jsa.or.jp/books/W11M0090/index/?bunsyo_id=JIS+C+0617-8%3A2024)
で**「PDFをバッグに入れる」**（冊子と同額なので選び間違えに注意）。
買ったら `%USERPROFILE%\規格\` に置く。**リポジトリに入れない。**

**道具はほぼそのまま使える。** `paths.standard(8)` と `paths.index(8)`、
`stdpage.Page(<ページ>, 8)`、`compare_page.py --part 8` はもう部を受け取る。
**直すのは2つだけ。**

| 直すもの | なぜ |
|---|---|
| `docs/第8部索引.tsv` を作る | `paths.index(8)` が読む。番号→ページの対応表。第7部のを真似る |
| `tools/status.py` の `INDEX` と見出し | 第7部を焼き付けてある（26・69・166行あたり）。部を受け取るようにする |

`cheatsheet.py` の表題も「第7部」と書いてあるので、第8部を載せるときに直す。

作るのはこの順。**66記号すべては要らない。実務で使うものから。**

| 優先 | 記号 | 見当 |
|---|---|---|
| 1 | 表示灯（一般図記号） | 08-10-01 |
| 2 | 表示灯の色ちがい | 色は `<informations>` に書き、形は同じ |
| 3 | ブザー・警報器 | 08-10 のあたり |
| 4 | 電圧計・電流計 | 08-02 のあたり |

**色をどう持たせるかを先に決める。** 既存図面は緑・赤・黄・青を別部品にしていた。
`.elmt` に色を焼くか、1つにして図面上で変えるかは
[docs/寸法基準.md](docs/寸法基準.md) の「線」（色は付けない、表示灯は例外）を見て決める。

### 2. 第6部 JIS C 0617-6:2024（¥15,180）

直流モーターと整流器。**第8部を作り終えてから。**

### 3. あわせてやること（規格票が要る）

- **押しボタンのブレーク接点**（上の「要確認」）
- **規格票の補足事項・形状分類・注釈の通し読み。**
  07-15-16 の黒丸のような「落とし」が他にもないか。第7部161個ぶん、まだ読んでいない

```bash
py -3 -c "import sys;sys.path.insert(0,'tools');sys.stdout.reconfigure(encoding='utf-8');import paths,pypdfium2 as p;print(p.PdfDocument(paths.standard())[107].get_textpage().get_text_range())"
```

## 次の一手（会社）—— 使ってみる

記号は 212個入る（旧33＋今回179）。引くときは
[docs/カタログ.md](docs/カタログ.md)（全部の姿）と
[docs/チートシート.svg](docs/チートシート.svg)（よく使うものの A4 1枚）。
気づいたことは [docs/点検メモ.md](docs/点検メモ.md) に書く。

**新しい作図用部品3つを実機で確かめる。**

フォリオ参照は**決着済み。動く。**ただし順番がある。

1. **先に** プロジェクトの属性 →「新しいフォリオ」→「フォリオ参照のラベル」を
   **`%id-%l%c`** にする（`%f` は空になることがある）
2. **そのあとで**フォリオを作る。**この設定は既存のフォリオには効かない**
3. 札を置いて相手とリンクする（右クリック →「リンクする要素」）

詳しくは `.claude/skills/qelectrotech/SKILL.md` の「フォリオ参照」。

**ラベルと相互参照も確認済み。**コイルを `R1` にすれば、ラベルを書いていない
接点にも `R1` が出て、脇に相手の位置 `(1-B4)` が付く。記号側に文字の欄
（`ElementInfo`／`label`）があるからで、こちらの記号 153/179 に入っている
（残り26は端子を持たない限定図記号と作図用の端子。機器記号を書かないもの）。

> **`.qet` を生成するとき、空の `<dynamic_texts />` を書かない。**
> **定義側のラベル欄まで打ち消される。**これで何も出なくなり、
> 「スレーブのラベルは同期されない」と2度読み違えた。
> **手で描くと動くのに生成すると動かないときは、まず生成側を疑う。**

相互参照の位置も **`%f` ではなく `%id`**（`qetgen.py` の `<xref>` は修正済み）。

> **推測で2回外した。**「線番が一致すれば結ぶ」も「0.100 では動かない」も違った。
> 既存フォリオで出ないのを見て「動かない」と結論づけたのが誤り。
> **依頼者が新しいフォリオを作って出た。**
> 公式文書（qelectrotech-doc の element/type）に `%id-%l%c` と
> 「New folio section」と書いてある。**手元で再現しないときは、
> その設定がいつ効くのか（新規だけか既存もか）を疑う。**

### 突き合わせは**全件終わった**

寸法の全件走査に加えて、格子の無い第16節8個も目視で見比べた。**形はすべて一致。**
残る「ちがう」は5件で、すべて理由が付いている。

| 残っているちがい | 件数 | 理由 |
|---|---|---|
| 引出し線の長さ | 3 | 端子間 60 の決めごと（07-13-14・07-13-15・07-13-16） |
| 可動接片の角度 | 1 | 決めごと（07-25-01） |
| 規格票に図が2つ | 1 | 07-25-03 |

**07-01-10（断路遮断機能）だけ保留。** 規格票の自シートでは 1.26×1.04M だが、
同じ星を組み込んだ 07-13-17 では 1.0M。**規格票のほうが揃っていない。**
こちらは 1.0M にしてある（組み込んだときに揃うほうを採った）。

**書体は合わせないと決めてある。** 規格票は変数をセリフ斜体で組む（`U` `I` `P`）。
こちらは Liberation Sans。形は合わせるが書体は合わせない。

**道具はリポジトリに入れた**（この回で入れた。もう作り直さなくてよい）。

```bash
py -3 tools/stdpage.py 78 --rows        # 格子・墨の成分・行走査
py -3 tools/compare_page.py 07-13-17    # 規格票と並べる
py -3 tools/compare_page.py --num --all # 全件の数値。数分かかる
```

### 直した記録（この突き合わせで出たもの）

- **測定そのものが壊れていた。** 格子の1目を「間隔の中央値」で決めていて、
  p.78 で **1M が半分**、p.81 で **7% 大きく**出ていた。周期合わせに変えたら
  「要修正」と見ていた 07-13-17・07-22-02 は**どちらも正しかった**
- **07-13-14** 破線が円の中から出て、4極目の先まで伸びていた
- **07-15-16 / -17 / -23** 規格票の**黒丸2つ**を落としていた（形状分類が「点」）
- **07-13-12** **戻り止め付き手動引外し装置**を落としていた（補足事項に書いてある）
- **記号の中の文字が全体に小さすぎた。** 規格票はどのページも字面 0.90〜0.93M。
  6〜8pt が混ざっていたのを **66か所 9pt に**そろえた
- **07-12-06** 文字を大きくしたら行が重なった。箱が 2M 低く、行間も 0.95M（規格票 1.27M）
- **07-15-09** 箱が左に 0.5M 短かった
- **07-13-15** 上端の縦棒が 0.2M（規格票 0.65M）
- **描き出しの破線が QET の4倍細かかった**（刻み 0.25M 対 0.6M）。
  QET は Qt のペンそのままで**刻みを指定できない**。規格票は 1.0M だが、
  同梱の IEC コレクションも 113個が同じ指定で済ませているので据え置き
- `check_elmt.py` の文字判定を、送り幅の表から**実フォントの実測**に変えた

### 見つかったら

**生成し直さず XML を書き換える。** uuid が変わると既存の `.qet` の参照が切れる。

## そのあとの候補

どれも規格票が要る。[docs/作業計画.md](docs/作業計画.md) 参照。

- 第8部 表示灯（08-10-01 ほか）。**規格票が未購入**（¥11,660）
- 第2部・第3部の常用記号

宿題として残っていること。

- **9位置以上の多段スイッチ**（07-11-06）。2〜8位置は作ってある。
  それ以上が要るときは `_8` と同じ規則で起こす（生成スクリプトは使い捨てなので書き直す）

- 07-25-03 は規格票が電流の向きの違う図を2つ載せている。1個だけ描いてある
  （QET で180度回せば逆向きになる）。要るなら逆向きも足す
- 作図用部品の `link_type` は **`simple`** にした。[docs/寸法基準.md](docs/寸法基準.md) は
  `terminal` と書いているが、それだと端子台の一覧に分岐点まで並ぶと踏んだため。
  **端子台は QET 本体ではなく「端子台作成プラグイン」だった**ので当面は問題なさそう。
  実際にそのプラグインを使うときに、分岐点が一覧に出ないかを見る

## 一つ描くときの手順

1. `py -3 tools/status.py --next 10` で番号とページを出す
2. `py -3 tools/stdpage.py <ページ>` で測る。**規格票の説明欄も読む**
3. `tools/elmtgen.py` を使う生成スクリプトを**スクラッチパッドに**書いて `.elmt` を吐く
   置き場所は `elements/JIS_C_0617/<節>/<番号>_<和名>.elmt`（**節のフォルダ名はASCII**）
   節のフォルダを新設したら `qet_directory` も置く（表示名はここが持つ）
4. `py -3 tools/render_elmt.py <番号>` で**必ず目視**
5. `py -3 tools/check_elmt.py` と `py -3 tools/compare_page.py <番号>`
6. `py -3 tools/status.py` → `git commit` → `git push`

## 測る道具

規格票は**リポジトリの外**（`%USERPROFILE%\規格\`）。`.gitignore` で二重に弾いている。
在りかは `paths.standard()` が解決するので、**パスをソースに書かない。**

| | |
|---|---|
| `tools/stdpage.py` | 図の帯と格子を取り出す。ライブラリとしても使う |
| `tools/compare_page.py` | 記号を規格票と並べて寸法を数値で出す |

## 見せる道具（規格票が要らない。会社PCでも使える）

```bash
py -3 tools/catalog.py       # docs/カタログ.md と docs/images/*.svg
py -3 tools/cheatsheet.py    # docs/チートシート.svg（A4 1枚・印刷用）
py -3 tools/checklist.py     # docs/点検メモ.md（176個ぜんぶにメモ欄）
```

**記号を足したり直したら `catalog.py` を叩く。**`status.py` が採録の「数」を
数え直すのと同じで、**カタログを手で書かない。**
チートシートは載せるものを `cheatsheet.py` の `GROUPS` が持っていて、
**A4 に収まらなくなると終了コード1で知らせる**（足すなら何かを落とす）。

**[docs/点検メモ.md](docs/点検メモ.md) は書き込むための表。** 176個ぜんぶに
「済」と「気づいたこと」の欄がある。**書き込んだものは `checklist.py` を
叩き直しても消えない**（番号で引き継ぐ）ので、記号を足したあとも安心して叩ける。
直したらその行を空に戻す。

**書くときは Excel から**（`--xlsx`）。記号の姿を見ながら書いて、
引数なしで叩き直すと `.md` に写る。ただし**`.xlsx` は git に入れていない**
（バイナリで差分が出ず、2台のPCで別々に書くと突き合わせができない）。
**載るのは `.md` のほう。**紙で書きたいときは `--print`。

**もう1台では Excel をコピーしない。** 持ち歩くのは `.md` で、Excel は現地で組み直す。

```bash
git pull
py -3 tools/checklist.py --xlsx    # .md の書き込みごと Excel が組み上がる
```

書いたら `py -3 tools/checklist.py` で写して push する。
**Excel に書いたら pull の前に写すこと。** 写さずに pull すると `.md` が
新しくなる。**そのときは止まる**ので黙って消えはしないが、
どちらを採るか選ぶ手間が出る（`--from xlsx` / `--from md`）。

> かつてブラウザに局所サーバを立てて書く方式も作ったが、**使わないので消した。**
> 見た目（節の見出し・12pt・淡い罫線）は Excel 側に移してある。

**使い捨ての測定スクリプトを書く前に `stdpage.py` を import する。**
`Page(78)` が帯・格子・墨の成分を持っている。素朴に書き直すと
下の落とし穴を踏み直す（実際に2回落ちた）。詳しくは [docs/ツール.md](docs/ツール.md)。

## つまずいた実例（同じ穴に落ちないため）

- **規格票の図は縦横で縮尺が違う。** 表のセルに入っているせいで列と行の間隔が
  最大6%ずれる。**必ず別々に測る**
- **格子の1目を「間隔の中央値」で決めてはいけない。** ドットは図に隠れて飛ぶし、
  図の小さな部品や表の切れ端がドットに混じる。中央値も最頻値もそれに引きずられ、
  **あるページで 1M が半分、別のページで 7% 大きく**出た。
  **周期そのものを合わせる**（Σ exp(2πi x/p) が最大の p を探す。`stdpage.py`）
- **図の帯は横罫線と横罫線のあいだ。**最初の罫線で切ると見出し行で切れる。
  帯をドットの**数**だけで選ぶと、説明表の句読点のほうを図と取り違える
- **`text` の中身はエスケープする。** `I >` の `>` を素で書くとタグの終わりと
  見分けがつかず、読み手が途中で切って**文字が丸ごと消える**
- **弧の angle が負のとき向きが逆になる。** Pillow は start から時計回りに end まで
  描くので、Qt の負角をそのまま渡すと反転する（`render_elmt.py` で対処済み）
- **可動接片の角度は規格票がページごとに揺れている。** 揃えると決めてある。
  [docs/寸法基準.md](docs/寸法基準.md) の表に従う
- **規格票にリード線が無い図記号がある。** 外形の縁に端子を足し、
  足したことを `<informations>` に必ず書く
- **目視だけでは外形の不備が残る。** 絵に出ないので気づけない。
  `check_elmt.py` を通したら、描き終えたつもりの166個から17件出た。
  **描いたら目視と `check_elmt.py` の両方をかける**
- **弧を外接矩形で判定すると必ずはみ出して見える。** 楕円の通らない側まで数えるため
- **描き出し側と生成側が同じ数式で間違えると、目視でも検査でも通ってしまう。**
  文字の `1em` を `0.78 × pt` としていた（正しくは `pt × 96/72`）。
  プレビューでは正しく見えて、**QET では1.7倍**になり外形からはみ出していた。
  **迷ったら同梱コレクションの統計と突き合わせる**（うちだけ 10〜18、同梱は 2〜9 だった）
- **文字が外形からはみ出すと、動かしたとき画面にゴミが残る。**
  QET は外形の矩形しか描き直さないため。見た目だけの問題ではない
- **リポジトリの外に書いたものは実ディスクに届かない**（上記）。
  QET のユーザーコレクションを「入れておきました」と言わないこと。**依頼者に叩いてもらう**
- **`elements/` 以下に日本語のファイル名・フォルダ名を作らない。** 実機で確かめた。
  ファイル名に日本語 → **記号名がファイル名（拡張子つき）に化ける**。
  フォルダ名に日本語 → **`qet_directory` が読めず名前が空欄**。
  和名は `.elmt` の中の `<name lang="ja">` と `qet_directory` が持つので表示は日本語のまま。
  `check_elmt.py` が弾く
- **外形 10×10 は使わない**（20×20 以上）。同梱8597個でも4個しかない外れ値。
  ただし**今回の「名前が化ける」の原因はこれではなかった**——原因はファイル名の日本語
- **症状を1つの原因で説明しようとしない。** 「フォルダ名が空」と
  「記号名がファイル名になる」は別々の原因だった。片方を直しても片方は残る
- **仮説を「外れ」と早々に切らない。** 依頼者の「日本語フォルダ名では？」が正解だった。
  こちらは一度否定していた
- **限定図記号の中をリード線が通らないことがある。** 07-19 のひし形は
  上下の頂点で線が切れている。**行走査で中心の x に暗い画素が無いか**を見れば分かる
- **測定メモを鵜呑みにしない。** 07-18・07-19 では控えの3か所（枠の下端・
  縦棒の長さ・被検出体の形）が実測と違った。描く前に**そのページをもう一度走査する**

## 触ってはいけないもの

- **`backup-before-scrub` ブランチ。**スクラブ前の履歴。**絶対にプッシュしない。**
  `git push --all` と `--mirror` を使わない
- 買った規格票の PDF と、そこから描き出した画像。リポジトリに入れない
